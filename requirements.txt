# Runtime dependencies for Video Grid Player
# Install with:  pip install -r requirements.txt
#
# Note: libvlc (the VLC native library) must also be present on the system.
#   macOS   : brew install --cask vlc        (or download from videolan.org)
#   Windows : https://www.videolan.org/vlc/download-windows.html
#   Linux   : sudo apt install vlc           (or distro equivalent)

PyQt6>=6.6.0
python-vlc>=3.0.20000
opencv-python>=4.8.0
